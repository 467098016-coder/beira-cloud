# 老板开机速查卡（贴在电脑旁）

> **就这 3 条命令，5 分钟上线。**  
> 跟着做就行，每步都不要跳过。

---

## 一次性安装（已经装好可以跳过）

我帮你装好了，你电脑里现在有：

- ✅ Node.js（让服务器跑起来）
- ✅ cloudflared（让工人能从外网进来）

所以你**现在不用装任何东西**。直接往下看 ↓↓

---

## 每次开机要做的 3 步

### 第 1 步：开第 1 个黑色窗口（启动服务器）

按键盘上的 **Windows 键**（左下角四个方块那个），输入 `cmd`，按回车。

会弹出一个黑底白字的窗口。

在窗口里 **复制粘贴这一整段**（一起复制，按回车）：

```cmd
cd "C:\Users\1\WorkBuddy AI\2026-08-04-17-27-47\beira-cloud"
node server.js
```

回车后会看到：

```
🚀 Beira Cloud · 老大手动跑版
   URL:    http://localhost:3000
   Health: http://localhost:3000/api/health
   Data:   C:\Users\1\WorkBuddy AI\2026-08-04-17-27-47\beira-cloud\data\store.json
```

**⚠️ 这个窗口不能关！** 关了服务器就停了，工人进不来。

---

### 第 2 步：再开第 2 个黑色窗口（启动网址隧道）

跟第 1 步一样：按 **Windows 键** → 输入 `cmd` → 回车。

在**新窗口**里复制粘贴：

```cmd
cd "C:\Users\1\WorkBuddy AI\2026-08-04-17-27-47\beira-cloud"
npm run tunnel
```

回车后等 5-10 秒，会看到一堆字，最后一行类似：

```
+-----------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at:         |
|  https://XXXX-YYYY-ZZZZ.trycloudflare.com                 |
+-----------------------------------------------------------+
```

**那个 https://...trycloudflare.com 的链接就是要发给工人的网址。**

右键窗口标题栏 → 编辑 → 复制，能把这一行抄出来。

**⚠️ 这个窗口也不能关！**

---

### 第 3 步：自己先测一下

在**第 3 个窗口**（或者浏览器）打开这个网址试试：

```
http://localhost:3000
```

应该看到登录页：
- 用户名输入 `admin`
- 密码输入 `admin123`
- 点登录

进了系统就 OK 了。

**✅ 给工人的账号**：
- 用户名：`worker1`
- 密码：`pass123`
- 叫他打开第 2 步那个 `trycloudflare.com` 的链接

---

## 收工前

- 两个窗口都**最小化**（不要关）
- 想关掉服务器就把两个窗口都关掉

---

## 常见问题

### Q: 工人说"连接不上"
看第 2 步那个窗口还在不在，不在就重新跑：
```
cd "C:\Users\1\WorkBuddy AI\2026-08-04-17-27-47\beira-cloud"
npm run tunnel
```
**每次启动那个 trycloudflare.com 链接都会变**，要重新发给工人。

### Q: 我重启电脑了会怎样？
两件事都得重新跑（跑回第 1 步、第 2 步）。

### Q: 数据存在哪？
在 `C:\Users\1\WorkBuddy AI\2026-08-04-17-27-47\beira-cloud\data\store.json` 这个文件里。  
每次写操作都会自动备份到 `store.bak.json`。  
**建议你每周把这个文件夹复制到 U 盘**，万一电脑坏了不会丢数据。

### Q: 工人说"我看不到柜子的收入/利润数字"
这是正常的 —— worker 账号是看不到这些的（看成本可以）。  
要看收入/利润，用 admin 账号登录。

---

## 你只需要记住

1. **开电脑** → 跑第 1 步
2. **再开一个窗口** → 跑第 2 步
3. **把那个 trycloudflare.com 的网址发给工人**，附账号 `worker1 / pass123`
4. **不要关这两个窗口**
